{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "ng build --prod",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
